import { FiltroPasajeroPipe } from './filtro-pasajero.pipe';

describe('FiltroPasajeroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPasajeroPipe();
    expect(pipe).toBeTruthy();
  });
});
